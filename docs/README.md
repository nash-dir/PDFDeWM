# docs/ — README assets

Place `gui.png` here (referenced from the top of the project README).

**What to capture:** the main window mid-workflow — file list populated, a scan
finished so the candidate panel shows a mix of image and text thumbnails, with a
page preview open on the right. That single frame conveys detection, selection,
and preview at a glance. A short GIF of scan → select → remove can replace it as
`gui.gif` (update the README link to match).
