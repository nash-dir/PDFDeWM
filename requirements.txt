# Runtime dependencies. Kept in sync with [project.dependencies] in pyproject.toml.
# Prefer `pip install .` for normal use; this file is provided for convenience.
PyMuPDF>=1.24,<2.0
Pillow>=10.0,<12.0
